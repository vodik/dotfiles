[user]
	name = Simon Gomizelj
	email = simongmzlj@gmail.com
[code]
	editor = vim
[color]
	ui = on
[merge]
	tool = vimdiff
	log = true
[push]
	default = current
[help]
	autocorrect = 1
[log]
	decorate = full
[diff]
	rename = copies
[web]
	browser = aurora
[browser "aurora"]
	cmd = aurora
[alias]
	co = checkout
	br = branch
	purr = pull --rebase
	stat = status -sb
	ignored = ls-files --others -i --exclude-standard
	wipe = reset --hard HEAD
[branch "master"]
	remote = origin
	merge = master
[branch "upstream"]
	remote = upstream
	merge = master
[remote "origin"]
	fetch = +refs/pull/*/head:refs/remotes/origin/pull/*
[sendemail]
	smtpserver = smtp.gmail.com
	smtpserverport = 587
	smtpencryption = tls
	smtpuser = simongmzlj@gmail.com
	confirm = auto

# vim: set ft=gitconfig noet:
